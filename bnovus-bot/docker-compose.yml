services:
  bot:
    build: .
    env_file: .env
    environment:
      - TZ=America/Santiago
    volumes:
      - ./src:/app/src
      - ./screenshots:/app/screenshots
      - ./downloads:/app/downloads
      - ./session:/app/session
    restart: "no"
