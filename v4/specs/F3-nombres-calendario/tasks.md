# F3 — Tasks

- [x] Auditar query de calendario: `dayWorkerName` disponible en CalendarView via `workerMap`
- [x] Vista global: celdas muestran `shortWorkerName(dayWorkerName)` en turno y en bloqueo
- [x] `shortWorkerName()`: primer nombre + inicial apellido
- [x] Encabezado de fila: muestra nombre completo del vendedor asignado al slot

## Estado: COMPLETO ✓
